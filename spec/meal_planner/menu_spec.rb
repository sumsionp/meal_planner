require 'meal_planner/menu.rb'
require 'timecop'

describe Menu do
  before do
  end

  it "adds a menu day"

  it "switches a meal with one that is not already in the list"
  # switch(menu_day)

end
